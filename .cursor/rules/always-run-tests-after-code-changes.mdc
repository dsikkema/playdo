---
description: Run tests after changing ANY Python code ALWAYS
globs: *.py
alwaysApply: false
---
# Run tests
After ANY code changes to a set Python files, run tests.

From the `backend` directory, run tests after changes to make sure they still pass.

If already in the backend directory, don't change directories. If not, cd into it:
```
cd backend
uv run pytest
```
